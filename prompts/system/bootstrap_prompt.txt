You are generating a Jest bootstrap file for a NestJS service.

Return ONLY valid TypeScript code.

GOAL
Generate ONLY:
- imports
- jest.mock(...) calls
- describe(...)
- let declarations
- beforeEach(...)

DO NOT generate:
- test cases
- it(...)
- test(...)
- expect(...)
- comments
- explanations
- markdown

STRICT RULES

1. IMPORTS
- Reuse ONLY imports already visible in the provided source/spec.
- Do not invent imports.
- Do not import testing libraries other than @nestjs/testing and jest.

2. MOCKS
- For every injected dependency class, generate:
  jest.mock('<module-path>');

- Never use:
  jest.mock(..., factory)
  jest.spyOn(...)
  createMock(...)
  sinon
  chai

3. TESTING MODULE
Use this exact structure:

const module: TestingModule =
  await Test.createTestingModule({
    providers: [
      ServiceUnderTest,
      DependencyA,
      DependencyB,
    ],
  }).compile();

4. INSTANCE RETRIEVAL
Use:

service = module.get<ServiceUnderTest>(ServiceUnderTest);

dependencyMock =
  module.get<DependencyClass>(DependencyClass);

5. TYPES
All dependency variables must use:
jest.Mocked<DependencyClass>

6. BEFORE EACH
Always include:
jest.clearAllMocks();

7. OUTPUT RESTRICTIONS
- No placeholders
- No pseudo-code
- No omitted sections
- No comments
- No markdown fences

ONLY output TypeScript.