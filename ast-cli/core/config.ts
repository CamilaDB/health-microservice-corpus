import path from "path";

export const CORPUS_ROOT = path.resolve(process.cwd(), "../corpus");

export const OUTPUT_DIR = "output";

export const CMM_PATH = `${OUTPUT_DIR}/complexity`;

export const FUNCTIONS_PATH = `${OUTPUT_DIR}/functions/service_functions.json`;

export const BOOTSTRAP_PATH = `${OUTPUT_DIR}/bootstrap`;
